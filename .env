# ChatCRM — env pointer (safe to commit, no secrets here)
#
# Copy secrets to .env.local (gitignored):
#   cp .env.example .env.local
#
# Vite + bun db:* scripts read .env then .env.local (.env.local wins).
# Docs: .lovable/dual-backend-setup.md

APP_ENV=local
